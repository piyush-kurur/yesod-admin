.highlight:hover
   box-shadow:0 0 #{shadowWidth} #{shadowColour}
.pushable:active
   box-shadow:inset 0 0 #{shadowWidth} #{shadowColour}

.shadow
   box-shadow:0 0 #{shadowWidth} #{shadowColour}

.branding
   background: #{penColour}
   color: #FFFFFF
   width: 100%
   height: 2em
   padding-top:#1em
   padding-bottom:#1em
   

.container
   background: #FFFFFF
